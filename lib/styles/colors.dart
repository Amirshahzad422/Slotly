import 'package:flutter/material.dart';

class AppColors {
  // Main & Background Colors
  static const Color primaryBackground = Color(0xFFEDEDCF);
  static const Color white = Colors.white;

  // Text & Icon Colors
  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textLight = Color(0xFF94A3B8);
  static const Color iconPrimary = Colors.black87;

  // Border & Shadow Colors
  static final Color borderLight = Colors.white.withValues(alpha: 0.7);
  static final Color shadowDark = Colors.black.withValues(alpha: 0.2);
}
